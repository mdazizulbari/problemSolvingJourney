#include <stdio.h>

int main(){

    int total_students;
    total_students = 20;
    total_students = 30;
    total_students = total_students + 5;
    printf("Total number of students %d\n", total_students);

    double gpa = 3.6;
    printf("My cgpa %lf\n", gpa);

    return 0;
}