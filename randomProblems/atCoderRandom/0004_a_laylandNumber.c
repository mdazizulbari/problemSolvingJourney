// https://atcoder.jp/contests/abc320/tasks/abc320_a?lang=en
#include <stdio.h>
// #include <math.h>

int main() {

    int a,b;
    scanf("%d%d", &a, &b);

    // int ans = (pow(a,b) + pow(b,a));

    // printf("%d\n", ans);
    
    return 0;
}